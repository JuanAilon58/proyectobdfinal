server.port=8081
spring.application.name=catalog-service

# Tomcat Tuning
server.tomcat.threads.max=300
server.tomcat.accept-count=500
server.tomcat.max-connections=5000

# PostgreSQL Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/flashbuy}
spring.datasource.username=admin
spring.datasource.password=admin123
spring.datasource.hikari.maximum-pool-size=30
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Redis Configuration
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:redis}
spring.data.redis.port=6379
spring.cache.type=redis
spring.cache.redis.time-to-live=600000

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.prometheus.enabled=true
management.endpoint.health.show-details=always
management.metrics.export.prometheus.enabled=true

# Habilita la inicialización de la base de datos con scripts SQL
#spring.sql.init.mode=always

# Si tu init.sql CREA las tablas (DDL), usa esta línea:
#spring.sql.init.schema-locations=file:C:/Base de Datos II/DBPF/proyectobdfinal/final/database/init.sql

# Si tu init.sql solo INSERTA datos (DML) porque las tablas ya existen, usa esta:
# spring.sql.init.data-locations=classpath:init.sql